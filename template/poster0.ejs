<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <h4>Hello! <%= name%> , you have shared some files to your friends.</h4>
    <table border="1" cellpadding="0" cellspacing="0" width="100%">
        <tr>
            <th> File Name </th>
            <th> File Size </th>
            <th> Effective Date </th>
        </tr>
        <%if (files.length>0) { %>
            <% files.forEach(function(file){ %>
             <tr><td><%= file.name %></td><td><%= file.size %></td><td><%= file.effectiveDate %></td></tr>
            <% })%>
        <% }%>
    </table>
    <h4>The email distribution as below</h4>
    <table>
        <tr><th>email distribution</th><td><%=emails %></td></tr>
    </table>
</body>
</html>